# Website Project

A personal website with blog, projects, and terminal interface.

## Structure

- `content/` — Markdown files
- `shell/` — Terminal SPA template
- `build/` — Content pipeline
- `public/` — Static assets

## Development

```bash
terminalx dev
```

## Deployment

```bash
terminalx build
# Upload dist/ to GitHub Pages
```
